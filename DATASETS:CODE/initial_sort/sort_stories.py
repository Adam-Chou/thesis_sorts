#import packagesimport pandas as pdimport osimport time#filepathdir_path = os.path.dirname(os.path.realpath(__file__))#configure show all columnspd.set_option('display.max_columns', None)#upload covid storiess_c = pd.read_csv('original_data/media_covid.csv')s_e = pd.read_csv('original_data/media_ebola.csv')#edit media list to drop all inconvenient columns    #create array of strings to filterdelete_list = ["stories_id","media_id","media_inlink_count","inlink_count","outlink_count","post_count","author_count", "channel_count"]    #delete thems_c = s_c.drop(columns=delete_list)s_e = s_e.drop(columns=delete_list)#group together by dates and display chunks on line graphdate_sort = { "title":[], "media_outlet":[], "url":[], "date":[], "day":[], "month":[], "year":[]}media_outlet_sort = {}no_date = []#call start for timestart_time = time.time()for i in range(0,len(s_c)):#1. get date    temp_date = str(s_c.iloc[i]["publish_date"]).split()[0]    #2. check if in dictionary    #make sure it is a phrase we understand    if temp_date != 'nan':        date_sort["title"].append(s_c.iloc[i]["title"])        date_sort["media_outlet"].append(s_c.iloc[i]["media_name"])        date_sort["url"].append(s_c.iloc[i]["url"])                #dates        month_year = temp_date[0:7].split("-")                date_sort["date"] = temp_date[0:7]        date_sort["day"].append(temp_date[8:])        date_sort["month"].append(month_year[1])        date_sort["year"].append(month_year[0])    else:        no_date.append(s_c.iloc[i])print(date_sort.keys())print()#call end for timefinish_time = time.time()print("finished:", finish_time - start_time)print("no dates count:",len(no_date))#make a dataframe for  # https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.DataFrame.from_dict.html# for i in date_sort.values():#     print(i)print()c_months_df = pd.DataFrame.from_dict(date_sort)#sort by descendingc_months_df.sort_values(by=['date'], ascending=False)print()print(c_months_df)#instantiate date columns for new data setdate_cols = []for i in c_months["date"]:    if i not in date_cols:        date_cols.append(i)#create models for news stories#create models for facebook shares.#send files out# c_months_df.to_csv(os.path.join(dir_path,'edited_data/media_covid_edited1.csv'))#isolate urls#create/copy webscraper to get article and meta data #export text data into file, either json or csv#run sentiment analysis on it?#Use IBM Watson, according to Abdulllah